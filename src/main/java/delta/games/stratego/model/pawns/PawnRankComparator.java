package delta.games.stratego.model.pawns;

import java.util.Comparator;

/**
 * Comparator for pawns, using their rank.
 * @author DAM
 */
public class PawnRankComparator implements Comparator<Pawn>
{
  @Override
  public int compare(Pawn o1, Pawn o2)
  {
    return 0;
  }
}
